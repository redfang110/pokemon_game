This program generates a map (described below), 
    randomly places a player character on a road,
    and then generates a movement map for a hiker character and rival character

The program then asks if you would like to quit the program or enter the game loop
    if the game loop is entered the program does as described below

This is a program that generates a 401x401 world.
Each unit of the world is made of 80x21 maps.
Each map is made up of 
    (at least 2 regions) small grass (.), 
    (at least 2 regions) tall grass (:), 
    forests (or trees) (^), 
    and mountains (or boulders) (%) with 2 roads going N-S and E-W, 
    a pokemart, 
    and a pokecenter. 
(internally toggled off) The player can navigate manually by inputting n, e, s, w 
    fly to a location by inputting f x y 
    or quit by inputting q

The program generates distance maps for characters, fills the map with characters, and runs an infinite loop to update the character's turns